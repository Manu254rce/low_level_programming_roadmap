; Assembly program that prints all syscalls in a Linux kernel

; The program includes a function that makes the shell to 
; execute 'strace -c' and append the output to a file from which
; the program reads and prints the syscalls on a screen.

